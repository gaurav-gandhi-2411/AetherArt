__pycache__/
*.pyc
.env
.env.*
hf_token.txt
.venv/
.venv*/
.vscode/
.idea/
.DS_Store
*.log
images/*.png
outputs/
.claude/
reports/eval_charts/*_2026*.png
reports/eval_partial_latest.json
reports/eval_run.log
data/lora/ukiyo-e/images/
data/lora/*/training_output/
reports/lora_comparison_gallery/
docs/hero_gen.log
docs/hero_tiles/

# Local model caches (loaded by app at runtime if present, else HF Hub is used)
models/

# Hero image generation candidates (scratch outputs, not for tracking)
docs/hero_candidates/

# Experiment raw images (120+ PNGs per experiment — too large for git; results.csv has paths)
reports/experiments/*/images/

# Python coverage artifacts
.coverage

# Build / install artifacts
aetherart.egg-info/

# Session scratch files (not for tracking)
cleanup-status.md

# LoRA training data (too large for git, regenerated locally)
data/lora/

# Machine-generated eval run artifacts (committed by exception only; April 2026 run committed manually)
reports/eval_results_20260531*.json
reports/eval_results_20260531*.md

# One-off latency diagnostic scratch output (docs/LATENCY_ROOT_CAUSE.md cites these by
# file:line as raw evidence, but they are throwaway instrumentation runs, not a durable
# report — matches the scripts/_*.py convention for scoped diagnostic scripts below)
reports/_latency_diag*

# LoRA checkpoint visual comparison outputs — large PNGs, out of scope for source control
reports/lora_checkpoint_eval_sdxl/

# Private / throwaway scripts (underscore prefix convention)
scripts/_*.py

# PR 12 smoke eval scratch outputs
reports/_pr12_smoke*
