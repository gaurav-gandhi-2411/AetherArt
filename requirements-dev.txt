-r requirements.txt

# dev & testing
pytest==8.4.2
pytest-cov==6.2.1
ruff==0.15.13
mypy==1.16.0

# Modal demo deployment — dev dependency only, NOT in requirements.txt
# (Modal SDK is not a production runtime dependency)
modal==1.4.3

# eval chart generation — scripts/eval.py's generate_charts() imports this; never listed
# here before, so it was silently satisfied by the local conda env and every CI run that
# reached the chart-generation step failed with ModuleNotFoundError (46-run history,
# 2026-07-23 through 2026-07-30). Version matches requirements-lock.txt:68.
matplotlib==3.10.6

# eval-only scorer deps — eval harness only, NOT production runtime
# These packages have unresolvable pip conflicts so they are NOT listed as direct
# pip install targets here; they are managed in .github/workflows/ci.yml.
# For local development, install in this exact order:
#   pip install hpsv2==1.2.0
#   pip install "image-reward==1.5" --no-deps
#   pip install "fairscale==0.4.13" "openai-clip==1.0.1" "timm==1.0.27"
#   pip install "pytest==8.4.2" "pytest-cov==6.2.1"     # restore after hpsv2 downgrade
#   cp $SITE/clip/bpe_simple_vocab_16e6.txt.gz $SITE/hpsv2/src/open_clip/  # missing data file
#
# Pinned versions:
#   hpsv2==1.2.0        — pulls pytest==7.2.0 (conflict) and timm==1.0.27
#   image-reward==1.5   — pins timm==0.6.13 (conflict); install --no-deps
#   fairscale==0.4.13   — builds from source; conflicts with system NCCL in CI (skip in CI)
#   openai-clip==1.0.1  — provides bpe_simple_vocab_16e6.txt.gz
#   timm==1.0.27        — explicit override of image-reward's timm==0.6.13 pin
#
# Post-install patches (hpsv2 1.2.0 bugs, Linux only):
#   SITE=$(python3 -c 'import site; print(site.getusersitepackages())')
#   # 1. bpe vocab file (missing from PyPI wheel):
#   cp $SITE/clip/bpe_simple_vocab_16e6.txt.gz $SITE/hpsv2/src/open_clip/
#   # 2. turtle import bug in factory.py line 8 (tkinter crash on headless Linux):
#   python3 -c "f=open('$SITE/hpsv2/src/open_clip/factory.py'); c=f.read(); f.close(); open('$SITE/hpsv2/src/open_clip/factory.py','w').write(c.replace('from turtle import forward\n',''))"
openai-clip==1.0.1
timm==1.0.27
