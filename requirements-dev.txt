-r requirements.txt

# dev & testing
pytest==8.4.2
pytest-cov==6.2.1
ruff==0.15.13
mypy==1.16.0

# eval-only scorer deps — eval harness only, NOT production runtime
# Version conflicts require special install sequence — see .github/workflows/ci.yml.
#   hpsv2==1.2.0 pulls timm==1.0.27; image-reward==1.5 pins timm==0.6.13 (conflict).
#   image-reward is installed --no-deps in CI to avoid timm downgrade.
#   fairscale==0.4.13 is needed locally for real ImageReward inference but is skipped
#   in CI because it builds from source and conflicts with system NCCL on ubuntu-22.04.
#   Scorer tests are fully mocked so CI does not require fairscale.
#   image-reward also requires apply_chunking_to_forward from transformers.modeling_utils
#   (removed in transformers>=4.39); aetherart/eval_ir.py injects a compatibility shim.
#
# Local install (one-time):
#   pip install hpsv2==1.2.0
#   pip install "image-reward==1.5" --no-deps
#   pip install "fairscale==0.4.13" "openai-clip==1.0.1" "timm==1.0.27"
hpsv2==1.2.0
openai-clip==1.0.1
timm==1.0.27
